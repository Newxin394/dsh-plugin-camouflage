# dsh-plugin-camouflage bundle patch
- insert:
    - id: client-camouflage
      name: 'dsh-plugin-camouflage'
      config:
        enabled: true
        userAgent: 'Cline/3.0.0'
        targetHosts:
          - '*'
        # 剥离 x-stainless-* 指纹头；中转站认官方 SDK 指纹时改成 false。
        stripStainless: true
        # 排查时打开，每次改写写一行日志。
        logRewrites: false
